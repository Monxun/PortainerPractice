apiVersion: v1
kind: Service
metadata:
  name: ms-underwriter
spec:
  ports:
  - port: 8071
  selector:
    app: ms-underwriter
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-underwriter
spec:
  selector:
    matchLabels:
      app: ms-underwriter
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ms-underwriter
    spec:
      containers:
      - name: ms-underwriter
        image: ms-underwriter
        envFrom:
          - configMapRef:
              name: aline-configmap
          - secretRef:
              name: aline-secrets
        ports:
        - containerPort: 8071
          name: ms-underwriter
